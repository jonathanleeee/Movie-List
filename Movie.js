var Movie = (props) => (
	<li>
		{props.aMovie.title}
	</li>
);



window.Movie = Movie;
// movie --> obj


//hw keep working on movie list by monday till level 2, easir to do level 3 before level 2, 0 1 3 2 levels orders

// how to create a repo and connect to this files